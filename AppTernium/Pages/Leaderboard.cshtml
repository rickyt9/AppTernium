@page
@using Newtonsoft.Json.Linq;
@model LeaderboardModel
@{
}

<div class="container d-flex align-items-center justify-content-between mt-5 mb-4">
    <div class="btn-group btn-group-toggle mode-buttons">
        <button type="button" class="btn btn-inactive btn-left btn-leaderboard">EXAMEN</button>
        <button type="button" class="btn btn-active btn-right btn-leaderboard">PRÁCTICA</button>
    </div>

    <h1 class="display-4 leaderboard-text">LEADERBOARD</h1>

    <div class="form-group mb-0">
        <select class="form-control rounded-pill time-select" >
            <option>SEMANAL</option>
            <option>MENSUAL</option>
            <option>GLOBAL</option>
        </select>
    </div>

</div>

<div class="table text-dark">
    <table class="table table-hover table-striped">
        <thead class="thead-dark">
            <tr class="">
                <th class="px-4">Rango</th>
                <th class="">Username</th>
                <th class="px-4">Puntuación</th>
            </tr>
        </thead>
        <tbody>
            @{
                int counter = 1;
                foreach (var item in Model.ListScores)
                {
                    <tr>
                        <td>
                            @counter
                        </td>
                        <td>
                            @item.username
                            @if (counter == 1) {

                            } else if (counter == 2) {

                            } else if (counter == 3) {

                            }
                        </td>
                        <td>
                            @item.score
                        </td>
                    </tr>
                    counter = counter + 1;
                }

            }
        </tbody>
    </table>
</div>